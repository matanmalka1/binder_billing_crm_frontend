# Sprint 1 Frontend Freeze

- Status: FROZEN
- Date: 2026-02-11
- Commit Hash: `86762b0`

## Scope Locked

Sprint 1 frontend scope is locked to:
- Login bootstrap (`POST /auth/login`)
- Read-only Dashboard (`GET /dashboard/overview`)
- Read-only Binders list (`GET /binders`)
- Read-only Clients list (`GET /clients`)
- Hebrew-only UI with RTL layout
- No domain mutations beyond login
